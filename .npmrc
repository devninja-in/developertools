# Skip optional dependencies to avoid native module compilation issues in CI
optional=false

# Skip postinstall scripts to avoid native module build failures in CI
ignore-scripts=true

# Use exact versions for more reliable builds
save-exact=true

# Speed up CI builds
prefer-offline=true
audit=false